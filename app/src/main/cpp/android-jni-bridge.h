/*
 * android-jni-bridge.h — Main public header for the mrboto JNI bridge
 *
 * This is the single include point for other C files (e.g. native-lib.c).
 * It re-exports all public APIs from the module-specific headers.
 */

#ifndef ANDROID_JNI_BRIDGE_H
#define ANDROID_JNI_BRIDGE_H

/* ── Include module-specific APIs ────────────────────────────────── */

#include "jni-registry.h"
#include "jni-helpers.h"
#include "jni-ui.h"

#endif /* ANDROID_JNI_BRIDGE_H */
